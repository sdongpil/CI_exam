name: Test

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up JDK
      uses: actions/setup-java@v2
      with:
        java-version: '17'  # 사용하는 Java 버전으로 설정하세요.

    - name: Build and test with Gradle
      run: |
        ./gradlew build
        ./gradlew test   
